const sum = require('./hello'); // the require(file_path) function will return a object/function ref of the class we are importing i.e. hello.js in this case.

// calling the Add() from hello.js
console.log(sum(91,19));